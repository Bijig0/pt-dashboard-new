describe("stok-alat-to-js", () => {
  describe("errors in the excel file", () => {
    it("returns an error if there are errors in the worksheet", async () => {
      expect(true).toBe(true);
    });
  });

  describe("no errors in the excel file", () => {
    it("creates js records from an excel file", async () => {
      const excelWorkbookFilePath = "./in/normal.xlsx";
    });

    it("creates js records for single sheets", async () => {
      expect(true).toBe(true);
    });

    it("creates js records for single sheets", async () => {
      expect(true).toBe(true);
    });
  });
});
