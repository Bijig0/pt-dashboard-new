import type {
  CorrectCompanyName,
  Row,
} from "./generate-rekapan/group-company-typo-names/types";

const reclusterWithPrevMonthCorrectCompanyNames = (
  currentMonthClustered: string[][],
  prevMonthCorrectCompanyNames: Record<CorrectCompanyName, Row>
) => {
  // Get all
  // Delete all
  // Add all

  return;
};

export default reclusterWithPrevMonthCorrectCompanyNames;
