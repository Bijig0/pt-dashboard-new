{
  "name": "@pt-dashboard/database",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate:types": "supabase gen types typescript --project-id lkxwausyseuiizopsrwi > src/types.ts",
    "generate:zod": "supazod -i src/types.ts -o src/schemas.ts",
    "generate": "pnpm generate:types && pnpm generate:zod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "supazod": "^4.5.0",
    "typescript": "^5.0.0"
  }
}
